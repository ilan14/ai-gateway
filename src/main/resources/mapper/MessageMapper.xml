<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lava.ai_gateway.mapper.MessageMapper">

    <insert id="insert" parameterType="com.lava.ai_gateway.entity.MessageEntity" useGeneratedKeys="true"
            keyProperty="id">
        INSERT INTO messages (session_id, role, content)
        VALUES (#{sessionId}, #{role}, #{content})
    </insert>

    <select id="findBySessionId" resultType="com.lava.ai_gateway.entity.MessageEntity">
        SELECT id, session_id, role, content, created_at
        FROM messages
        WHERE session_id = #{sessionId}
        ORDER BY created_at ASC
    </select>

    <delete id="deleteBySessionId">
        DELETE FROM messages WHERE session_id = #{sessionId}
    </delete>

</mapper>
